apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: model-server
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: model-server
            port:
              number: 80