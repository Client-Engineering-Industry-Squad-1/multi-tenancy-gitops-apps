apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: document-understanding
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: document-understanding
            port:
              number: 9443